services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_MAPBOX_TOKEN: ${NEXT_PUBLIC_MAPBOX_TOKEN}
        NEXT_PUBLIC_MAPBOX_STYLE_ID: ${NEXT_PUBLIC_MAPBOX_STYLE_ID}
        NEXT_PUBLIC_MAPBOX_ADMIN_STYLE_ID: ${NEXT_PUBLIC_MAPBOX_ADMIN_STYLE_ID}
    env_file:
      - .env.local
    environment:
      DATABASE_URL: postgresql://alfgow:D210512d@host.docker.internal:5432/inmobiliaria?schema=public
      NODE_ENV: production
      PORT: 3004
      HOSTNAME: 0.0.0.0
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "3004:3004"
